/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:"blacvolta.com",
            }
        ]
    }
};

export default nextConfig;
